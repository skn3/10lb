// =============================================================================
// ROUND STATUS ENUM
// =============================================================================
export const RoundStatus = Object.freeze({
  ACTIVE: 'active',
  COMPLETED: 'completed',
  PENDING: 'pending'
});
